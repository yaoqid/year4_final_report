\documentclass[12pt]{final_project_report}
\usepackage{amsmath,amsthm,graphicx,subcaption}
\usepackage{booktabs}

\title{Modelling terahertz pulse generation due to ultrafast photocurrents in semiconductors }
\author{Yaoqi Dong, Yuhao Chang, Shenglong Li}
\date{2 May 2026}
\supervisor{Professor Oleg Mitrofanov }
\aiuse{{\bf If you have not used generative AI make sure that {\tt\bf\textbackslash aiuse} is empty. Only edit and include text inside {\tt\bf\textbackslash aiuse} if you have used generative AI.}

I acknowledge the use of name and version of generative AI system  Chat-GPT-  (Publisher OpenAI, URL of the AI system  \\https://chat.openai.com) to brief description of context in which AI tool was used to summarise my initial notes, help with LaTeX format and proofread my final draft.}
\wordcount{4401}

\begin{document}

\coverpage
\declaration
\maketitle
\begin{exec_summary}
This document provides a brief overview of how to use the 4\textsuperscript{th} year project final report \LaTeX\ template. It assumes that you have a basic understanding of \LaTeX.

Use the excutive summary to summarise your main aim of the project and your most important finding(s). Use one paragraph to briefly describe each finding or outcome. Make sure you include quantitative information if you can, e.g.\ the ML algorithm is 85\,\% accurate or the implemented amplifier has a gain of 25\,dB. Spark the reader’s interest!

The table of contents will appear on the next page.
\end{exec_summary}
\tableofcontents

\section{Introduction and Literature Review}

\subsection{Introduction}
The generation of terahertz radiation via the excitation of semiconductor surfaces with femtosecond laser pulses is a fundamental technique in modern photonics~\cite{Pettine2023}. This process relies on the acceleration of photo-generated carriers, where the resulting transient current acts as a Hertzian dipole source radiating electromagnetic waves into free space. While the physical mechanisms governing these currents—such as the Photo-Dember effect and surface depletion fields—are well-established in theoretical literature, the complex electrodynamics governing the subsequent propagation of the electric field often remain abstract and difficult to visualise.
\newline
The primary objective of this project is to bridge this gap by developing a dedicated simulation interface using MATLAB App Designer. Unlike standard numerical scripts, this graphical interface provides an interactive environment to model and visualise the generation of THz fields based on the method of Retarded Potentials. The specific motivation is to create a versatile tool that allows users to modify intrinsic semiconductor properties—such as carrier lifetime, diffusion coefficients, and absorption depth—to dynamically simulate the radiation characteristics of different materials. By transforming static theoretical equations into observable temporal waveforms and spatial vector maps, this interface serves as both a pedagogical tool for understanding surface emission physics and a predictive platform for optimising semiconductor materials in terahertz emitter design. Notably, recent experimental studies have demonstrated that THz emission spectroscopy can be used as a rapid, noncontact characterisation method for semiconductor surfaces~\cite{YangTonouchi2022}, underscoring the practical relevance of accurate THz emission models.

\subsection{Literature Review}
% TODO: Expand this into a proper literature review.
% - How is your project informed by existing knowledge?
% - How does existing knowledge relate to your project goals?
% - How will you use existing knowledge in your project?
% - How will you contribute to knowledge?
% Reference existing citations: \cite{Hebling:02}, \cite{SimulationTHz}, \cite{auston_picosecond_1984}

%% ============================================================
%% PROJECT GOALS
%% ============================================================

\section{Technical Background}

\subsection{Photo-Dember Effect}
% TODO: Move the theoretical explanation of the Photo-Dember effect here.
% Present the background theory, mathematical framework, and physical principles.
The model is based on the diffusion-recombination theory: a highly non-equilibrium carrier distribution is generated in the semiconductor by ultra-short laser pulses and then evolves under the driving force of spatial gradients and mobility differences. Existing literature has pointed out that the time derivative of the instantaneous photogenerated current is the main source of terahertz radiation~\cite{Hebling:02}. Recent comprehensive reviews of ultrafast carrier dynamics in terahertz photoconductors have further highlighted the central role of sub-picosecond transport phenomena in governing THz emission efficiency~\cite{Lu2022}. Therefore, the numerical framework of this project is based on this physical principle.
\subsection{Built-in Field Effect}
\subsection{Electric Field Theory}
\subsubsection{From Photocurrent to Electromagnetic Radiation}
The fundamental link between ultrafast photocurrents and terahertz emission lies in classical electrodynamics. When a femtosecond laser pulse excites a semiconductor surface, it generates a transient current density $\mathbf{J}(\mathbf{r}', t)$ through mechanisms such as the Photo-Dember effect and surface depletion field drift. Since this current varies on a sub-picosecond timescale, it radiates electromagnetic energy in the terahertz frequency range~\cite{Yang2023}. This principle was first experimentally verified by Auston et al.~\cite{auston_picosecond_1984}, who demonstrated that the far-field radiation from a transient current source is proportional to the first time derivative of the dipole moment. Recent studies have further shown that similar transient photocurrent mechanisms drive THz emission in semiconductor heterostructures, where ballistic photocurrents across band offsets produce measurable radiation pulses~\cite{Malevich2021}. The theoretical framework employed in this project treats each infinitesimal current element on the semiconductor surface as a Hertzian dipole, and the total radiated field is obtained by superposition, following the approach of Johnston et al.~\cite{SimulationTHz}.

\subsubsection{Retarded Potentials and Jefimenko's Equations}
The electric field at an observation point $\mathbf{r}$ due to a time-varying source distribution is rigorously described by Jefimenko's equations, which are the retarded-time solutions to Maxwell's equations~\cite{griffiths_electrodynamics}. The key physical insight is causality: the field observed at time $t$ depends on the state of the source at the earlier retarded time
\begin{equation}
    t_{\mathrm{ret}} = t - \frac{R}{c},
\end{equation}
where $R = |\mathbf{r} - \mathbf{r}'|$ is the distance from the source point $\mathbf{r}'$ to the observation point $\mathbf{r}$, and $c$ is the speed of light.

The full Jefimenko expression for the electric field contains three terms: a Coulomb (electrostatic) term proportional to $1/R^3$, an induction term proportional to $1/R^2$, and a radiation term proportional to $1/R$. In the far-field regime, where the observation distance is much larger than the spatial extent of the source ($R \gg d$), the $1/R^3$ and $1/R^2$ terms become negligible. The dominant radiative contribution is then
\begin{equation}
    \mathbf{E}(\mathbf{r}, t) \approx \frac{\mu_0}{4\pi} \iint_{S} \frac{\hat{\mathbf{R}} \times \left[\hat{\mathbf{R}} \times \dot{\mathbf{J}}(\mathbf{r}', t_{\mathrm{ret}})\right]}{R} \, dS',
    \label{eq:farfield}
\end{equation}
where $\hat{\mathbf{R}} = (\mathbf{r} - \mathbf{r}')/R$ is the unit vector from source to observer, $\dot{\mathbf{J}} = \partial \mathbf{J}/\partial t$ is the time derivative of the current density, and $\mu_0$ is the permeability of free space. The double cross product $\hat{\mathbf{R}} \times [\hat{\mathbf{R}} \times \dot{\mathbf{J}}]$ extracts only the transverse component of the current acceleration, ensuring that only radiation polarised perpendicular to the line of sight propagates to the far field. This formulation has been successfully applied to simulate terahertz generation at semiconductor surfaces by Johnston et al.~\cite{SimulationTHz}, who used a semiclassical Monte Carlo model to compute carrier dynamics and then evaluated the far-field radiation using the retarded potential framework. More recently, Kefayati and Nikoli\'{c}~\cite{Kefayati2024} demonstrated that Jefimenko's equations can be combined with time-dependent density functional theory to reveal the microscopic origins of terahertz radiation in spintronic emitters, further validating the generality of this approach for modelling ultrafast electromagnetic emission.

\subsubsection{Physical Interpretation}
Equation~(\ref{eq:farfield}) encodes several important physical features:
\begin{itemize}
    \item \textbf{Proportionality to $\dot{\mathbf{J}}$}: The radiated field is driven by the \textit{rate of change} of the current, not the current itself. A steady current does not radiate; only accelerating or decelerating charges produce electromagnetic radiation. This is consistent with the Larmor radiation formula in classical electrodynamics.
    \item \textbf{Retardation}: Each source element contributes to the observed field at a time delayed by $R/c$. For a spatially extended source, different elements contribute at different retarded times, producing temporal broadening and shaping of the observed pulse.
    \item \textbf{Geometric projection}: The double cross product ensures that the observed polarisation depends on the viewing angle. For a radially symmetric current distribution arising from the Photo-Dember diffusion mechanism, the transverse field components on-axis cancel by symmetry, while off-axis observation yields a net radially polarised signal.
    \item \textbf{$1/R$ decay}: The radiation field amplitude decreases as $1/R$, meaning the radiated power (proportional to $|\mathbf{E}|^2$) follows an inverse-square law, as expected for freely propagating waves.
\end{itemize}

In the context of this project, the current density $\mathbf{J}$ is obtained from the carrier transport simulation, which solves the diffusion-recombination equation for photo-generated electrons and holes in the semiconductor. The photocurrent arises from the spatial gradient of the carrier density via $\mathbf{J} = -qD\nabla n$, where $q$ is the elementary charge and $D$ is the diffusion coefficient. The Photo-Dember effect produces a net transient current because electrons diffuse faster than holes due to their higher mobility~\cite{Reklaitis2010}, creating a charge separation that radiates terahertz electromagnetic waves. Recent work by Hale et al.~\cite{Hale2022} has demonstrated that even ultrathin (160~nm) GaAs metasurfaces can generate THz pulses with efficiency comparable to bulk crystals, and Jung et al.~\cite{Jung2024} have further shown that InAs metalens emitters based on Mie resonators can produce focused THz beams with amplitudes up to 20 times greater than plasmonic emitters, highlighting how charge density gradients at nanostructured surfaces serve as effective radiation sources through similar photocurrent mechanisms.

%% ============================================================
%% METHODS
%% ============================================================
\section{Methods}

\subsection{Photo-Dember Numerical Model}
First, a one-dimensional electron diffusion-composition model was constructed. By applying Gaussian excitation in both space and time, the expected carrier behavior was successfully simulated: rapid accumulation at the excitation center, symmetric diffusion over time, and exponential decay. The model output was consistent with the analytical expectations, verifying the stability and physical correctness of the numerical method.\newline
Subsequently, the model was extended to two dimensions to reflect the circular symmetry of the real light spot. The two-dimensional results showed that the carriers diffused radially symmetrically and an electron density peak slightly delayed relative to the excitation peak appeared, which was consistent with the ultrafast carrier dynamics reported in the literature~\cite{Lu2022}. Further, by calculating the gradient of the electron density, the corresponding diffusion current was obtained. The result presented a typical transient current structure that radiated outward radially, which was in line with the characteristics of the Photo-Dember current.\newline
To enhance the physical accuracy of the model, additional holes were introduced and a double carrier coupling equation was constructed to reflect the mechanism of net current formation due to the difference in carrier mobilities between electrons and holes. The double carrier model shows that early transport is still dominated by electrons, while in the later stage, the spatial distribution of electrons and holes becomes more overlapping, resulting in a smoother overall carrier distribution, which is consistent with the existing experimental observations.

\subsection{Built-in Field Numerical Model}

\subsection{Electric Field Computation}
The numerical computation of the radiated electric field was implemented in MATLAB as a vectorised function (\texttt{compute\_THz\_E\_from\_Jx.m}) that accepts the full three-component current density history $\mathbf{J}(\mathbf{r}', t) = [J_x, J_y, J_z]$ from the carrier transport model and returns the time-domain electric field waveform at a specified observation point. The source current is generated by the function \texttt{simulateTHzFromDiffusion.m}, which solves the two-dimensional diffusion-recombination equation on a spatial grid and stores the current density components as three-dimensional MATLAB arrays of size $N_y \times N_x \times N_t$, where the first two dimensions correspond to the spatial grid and the third to the time steps. The computational model discretises the semiconductor surface into a grid of elementary Hertzian dipoles, and the total radiated field is obtained by coherently summing the retarded-time contributions from all source elements using the far-field approximation derived in Section~4.2.

\subsubsection{Discretisation and Geometry}
Figure~\ref{fig:geometry} illustrates the computational geometry. The semiconductor surface at $z = 0$ is represented by an $N_x \times N_y$ spatial grid spanning a domain of $L_x \times L_y = 1.5~\mathrm{mm} \times 1.5~\mathrm{mm}$, with the default resolution set to $N_x = N_y = 601$ grid points in the GUI implementation. The one-dimensional coordinate vectors $x$ and $y$ are generated using MATLAB's \texttt{linspace} function to produce uniformly spaced points from $-L/2$ to $+L/2$, and the two-dimensional coordinate matrices are constructed using \texttt{meshgrid(x, y)}, which returns matrices $XX$ and $YY$ of size $N_y \times N_x$. This convention, where the row index corresponds to $y$ and the column index to $x$, follows MATLAB's standard matrix orientation and is maintained consistently throughout both the carrier transport and field computation modules.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.75\linewidth]{interim_report_latex_template (1)/figure/yaoqqi/figure_geometry_schematic.png}
    \caption{
        Schematic of the far-field radiation geometry. The semiconductor surface is discretised into an $N_x \times N_y$ grid. Each source cell radiates via $\dot{\mathbf{J}}$ (green arrow). The field at the observer $\mathbf{r}$ is obtained by summing retarded-time contributions along $R_{ij}$.
    }
    \label{fig:geometry}
\end{figure}

Each grid cell at position $\mathbf{r}'_{ij}$ carries a time-varying current density $\mathbf{J}(\mathbf{r}'_{ij}, t_k)$ obtained from the diffusion-recombination solver. In the transport model, the diffusion current at each time step is computed from the spatial gradient of the carrier density using MATLAB's built-in \texttt{gradient} function, which applies second-order central differences internally:
\begin{equation}
    J_x = qD\frac{\partial n}{\partial x}, \quad J_y = qD\frac{\partial n}{\partial y},
\end{equation}
where $q$ is the elementary charge, $D$ is the diffusion coefficient, and $n$ is the carrier density. The \texttt{gradient} function was chosen because it handles boundary cells with appropriate one-sided differences, avoiding the need for manual ghost-cell implementations at the grid edges.\newline
The observation point $\mathbf{r} = (X_0, Y_0, z_0)$ is placed in the far-field region, typically at $z_0 = 5$--$10$~mm above the surface. For each source cell, the displacement vector $\mathbf{R}_{ij} = \mathbf{r} - \mathbf{r}'_{ij}$ defines the distance $R_{ij} = |\mathbf{R}_{ij}|$ and the unit vector $\hat{\mathbf{R}}_{ij} = \mathbf{R}_{ij}/R_{ij}$. These quantities are computed as two-dimensional arrays using element-wise MATLAB operations such as \texttt{sqrt} and \texttt{./}, which avoids any explicit loops over the spatial grid. A singularity guard is applied using \texttt{R(R==0) = eps}, where \texttt{eps} $\approx 2.2 \times 10^{-16}$ is MATLAB's machine epsilon, to prevent division-by-zero errors.

The surface integral in Equation~(\ref{eq:farfield}) is then approximated as a discrete sum over all source cells:
\begin{equation}
    \mathbf{E}(\mathbf{r}, t_k) \approx \frac{\mu_0}{4\pi} \sum_{i=1}^{N_x} \sum_{j=1}^{N_y} \frac{\hat{\mathbf{R}}_{ij} \times \left[\hat{\mathbf{R}}_{ij} \times \dot{\mathbf{J}}(\mathbf{r}'_{ij}, t_{\mathrm{ret},ij})\right]}{R_{ij}} \, \Delta x \, \Delta y.
    \label{eq:discrete}
\end{equation}

\subsubsection{Three-Stage Numerical Algorithm}
The evaluation of Equation~(\ref{eq:discrete}) is carried out through a three-stage pipeline, as summarised in Figure~\ref{fig:flowchart}. The input to the algorithm is the full spatiotemporal current density history $\mathbf{J}(x_i, y_j, t_k)$, stored as three MATLAB arrays \texttt{Jx}, \texttt{Jy}, and \texttt{Jz}, each of dimension $N_y \times N_x \times N_t$. The output is the three-component electric field waveform $\mathbf{E}(t)$, returned as three row vectors \texttt{Ex}, \texttt{Ey}, and \texttt{Ez} of length $N_t$.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.85\linewidth]{interim_report_latex_template (1)/figure/yaoqqi/figure_algorithm_flowchart.png}
    \caption{
        Flowchart of the three-stage algorithm. Stage~1 differentiates $\mathbf{J}$ in time. Stage~2 maps retarded times and interpolates $\dot{\mathbf{J}}$. Stage~3 applies the BAC-CAB identity. All source contributions are summed to yield $\mathbf{E}(t)$.
    }
    \label{fig:flowchart}
\end{figure}

\textbf{Stage 1: Temporal derivative.} The radiated field is proportional to $\dot{\mathbf{J}}$, not $\mathbf{J}$ itself, so the first step computes the time derivative of each current density component at every spatial grid point. A second-order central finite difference scheme is employed:
\begin{equation}
    \dot{J}_x(x_i, y_j, t_k) = \frac{J_x(x_i, y_j, t_{k+1}) - J_x(x_i, y_j, t_{k-1})}{2\Delta t},
\end{equation}
applied identically to $J_y$ and $J_z$. In MATLAB, this operation is performed on the entire three-dimensional array simultaneously using slice indexing without any explicit loops:
\begin{verbatim}
    dJx(:,:,2:Nt-1) = (Jx(:,:,3:Nt) - Jx(:,:,1:Nt-2)) / (2*dt);
\end{verbatim}
The notation \texttt{Jx(:,:,3:Nt)} selects all spatial grid points at time indices 3 through $N_t$, and the subtraction and division are applied element-wise across the full $N_y \times N_x \times (N_t - 2)$ sub-array in a single operation. This vectorised approach is significantly faster than an equivalent triple-nested loop, as MATLAB's internal array operations are executed in optimised compiled code. The second-order scheme was chosen over a first-order forward difference to minimise numerical dispersion errors in the derivative, which is critical because $\dot{\mathbf{J}}$ directly determines the amplitude and waveform shape of the radiated pulse. At the boundary time steps $k = 1$ and $k = N_t$, the derivative arrays are initialised to zero using \texttt{dJx = zeros(size(Jx))}, as the current density is negligible at the start and end of the simulation window.\newline
Figure~\ref{fig:dJdt} presents a representative snapshot of the resulting $|\dot{\mathbf{J}}|$ distribution across the source grid. The peak of $|\dot{\mathbf{J}}|$ occurs at $t \approx -0.03$~ps, which is a slightly negative time relative to the defined pulse centre at $t = 0$. This is physically expected because the simulation time axis is defined such that $t = 0$ corresponds to the peak of the Gaussian optical excitation pulse, and the time window extends from $T_{\mathrm{min}} = -5$~ps to $T_{\mathrm{max}} = 40$~ps to capture the full transient response. The current derivative peaks on the rising edge of the laser pulse because this is when the carrier generation rate, and hence the rate of change of the diffusion current, is increasing most rapidly. By the time the pulse reaches its maximum at $t = 0$, the generation rate has begun to plateau, causing $\dot{\mathbf{J}}$ to decrease. This temporal asymmetry is a fundamental characteristic of the Gaussian excitation profile and is consistent with the bipolar waveform structure observed in the radiated THz pulse.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.75\linewidth]{interim_report_latex_template (1)/figure/yaoqqi/figure_dJdt_snapshot.png}
    \caption{
        Spatial distribution of $|\partial \mathbf{J}/\partial t|$ at $t \approx -0.03$~ps. The ring-shaped pattern reflects the steepest carrier density gradients at the Gaussian beam flanks. White arrows confirm radially outward $\dot{\mathbf{J}}$ from Photo-Dember diffusion.
    }
    \label{fig:dJdt}
\end{figure}

Spatially, the heatmap reveals a distinctive ring-shaped magnitude distribution centred on the laser excitation spot. The magnitude $|\dot{\mathbf{J}}|$ is largest at intermediate radii where the spatial gradient of the carrier density is steepest, corresponding to the flanks of the Gaussian beam profile. At the beam centre, the carrier density is nearly uniform and the gradient is zero, so $\dot{\mathbf{J}}$ vanishes despite the high absolute carrier concentration. Similarly, far from the excitation region, the carrier density itself is negligible, so the derivative is also zero. The overlaid white arrows confirm that $\dot{\mathbf{J}}$ is directed radially outward at all azimuthal angles, reflecting the isotropic nature of the Photo-Dember diffusion mechanism. This radially symmetric source distribution has a direct physical consequence for the emitted radiation: when the far-field integral in Equation~(\ref{eq:discrete}) is evaluated along the surface normal at $\theta = 0^\circ$, the transverse projections from opposite sides of the ring cancel by symmetry, producing the characteristic on-axis null observed in both simulation and experiment~\cite{Mueckstein2015}.\newline
\textbf{Stage 2: Geometry and retarded time.} For each source cell $(i,j)$ and each observation time $t_k$, the retarded time is computed as $t_{\mathrm{ret},ij} = t_k - R_{ij}/c$, accounting for the finite speed of light over the source-to-observer distance $R_{ij}$. Since the retarded time generally does not coincide with a discrete simulation time step, it is converted to a fractional array index:
\begin{equation}
    \tau = \frac{t_{\mathrm{ret}} - t_1}{\Delta t} + 1,
\end{equation}
which maps the continuous retarded time onto the discrete time grid. The integer part $\lfloor\tau\rfloor$ identifies the two adjacent stored time steps that bracket the retarded time, computed in MATLAB using the \texttt{floor} function. Linear interpolation is then applied between these two steps:
\begin{equation}
    \dot{\mathbf{J}}_{\mathrm{ret}} = (1 - \alpha)\,\dot{\mathbf{J}}(\mathbf{r}'_{ij}, t_{\lfloor\tau\rfloor}) + \alpha\,\dot{\mathbf{J}}(\mathbf{r}'_{ij}, t_{\lfloor\tau\rfloor+1}),
\end{equation}
where $\alpha = \tau - \lfloor\tau\rfloor$ is the fractional part. The critical implementation challenge here is that $\dot{\mathbf{J}}$ is stored as a three-dimensional array, and the time index required for each spatial cell is different because each cell has a different distance $R_{ij}$ to the observer. A na\"ive implementation would require nested loops over all $N_x \times N_y$ cells, which would be prohibitively slow. Instead, the code constructs linear indices into the three-dimensional array using manual index arithmetic:
\begin{verbatim}
    idx1 = grid_y + (grid_x-1)*Ny + (k1_safe-1)*(Ny*Nx);
\end{verbatim}
The matrices \texttt{grid\_y} and \texttt{grid\_x} are pre-computed using \texttt{ndgrid} and contain the row and column indices for every cell. The variable \texttt{k1\_safe} is the clamped time index matrix. This expression converts the three-dimensional subscript $(j, i, k)$ into a single linear index in column-major order, allowing the entire interpolation to be performed as a single element-wise array operation:
\begin{verbatim}
    dJx_ret = (1-alpha).*dJx(idx1) + alpha.*dJx(idx2);
\end{verbatim}
This accesses the correct time slice for each spatial cell simultaneously without any loop.\newline
A boolean validity mask \texttt{valid = (k1 >= 1) \& (k2 <= Nt)} identifies source cells whose retarded time falls within the simulation window. Cells outside this range are clamped using \texttt{max(1, min(Nt, k1))} to prevent array index errors during interpolation, and their contributions are subsequently zeroed out with \texttt{dJx\_ret(\textasciitilde valid) = 0}. This two-step approach of clamping first and then masking avoids conditional branching within the vectorised computation while ensuring physically correct results.\newline
\textbf{Stage 3: Cross product and accumulation.} The double cross product $\hat{\mathbf{R}} \times [\hat{\mathbf{R}} \times \dot{\mathbf{J}}]$ in Equation~(\ref{eq:discrete}) extracts the transverse component of the current acceleration relative to the line of sight. Rather than computing two sequential cross products, the implementation exploits the BAC-CAB vector identity:
\begin{equation}
    \hat{\mathbf{R}} \times [\hat{\mathbf{R}} \times \dot{\mathbf{J}}] = (\hat{\mathbf{R}} \cdot \dot{\mathbf{J}})\,\hat{\mathbf{R}} - \dot{\mathbf{J}},
\end{equation}
which reduces the operation to a single dot product and two vector scalings. In MATLAB, this is implemented as:
\begin{verbatim}
    dJdot = Rhat_x.*dJx_ret + Rhat_y.*dJy_ret + Rhat_z.*dJz_ret;
    Tx = dJdot.*Rhat_x - dJx_ret;
    Ty = dJdot.*Rhat_y - dJy_ret;
\end{verbatim}
The variable \texttt{dJdot} holds the scalar dot product $\hat{\mathbf{R}} \cdot \dot{\mathbf{J}}$ computed element-wise over the full $N_y \times N_x$ grid, and \texttt{Tx}, \texttt{Ty} are the resulting transverse field components. The element-wise multiplication operator \texttt{.*} ensures that each spatial cell uses its own local values of $\hat{\mathbf{R}}$ and $\dot{\mathbf{J}}_{\mathrm{ret}}$, preserving the spatially varying geometry. This formulation is not only computationally more efficient but also numerically more stable, as it avoids the subtraction of nearly equal cross-product terms that can occur at small observation angles. The spatial summation is then performed using nested \texttt{sum} calls:
\begin{verbatim}
    Ex(k) = (mu0/(4*pi)) * sum(sum(Tx./R_mat)) * dx * dy;
\end{verbatim}
This sums the integrand over all $N_y$ rows and $N_x$ columns in a single expression, multiplied by the area element $\Delta x\,\Delta y$ to approximate the surface integral. The entire sequence of interpolation, cross product, and summation is executed once per observation time step $t_k$ within a \texttt{for} loop over $k = 1$ to $N_t$.

\subsubsection{Computational Performance}
The overall computational cost of the far-field calculation scales as $\mathcal{O}(N_x \cdot N_y \cdot N_t)$ per observation point. For the default GUI parameters of $N_x = N_y = 601$ and $N_t = 800$ output time steps over a window from $-5$~ps to $40$~ps, a single-point waveform calculation completes within approximately one minute on a standard desktop computer. The main performance bottleneck is the outer \texttt{for} loop over observation times $t_k$, which cannot be vectorised because the retarded time $t_{\mathrm{ret},ij}$ depends on the current value of $t_k$, meaning each iteration requires a different set of interpolation indices. However, all spatial operations within each time step are fully vectorised, operating on $N_y \times N_x$ matrices rather than looping over individual grid cells.\newline
The transport simulation uses a much finer internal time grid with $\Delta t_{\mathrm{fine}} = 0.5$~fs, because the forward Euler scheme used for the diffusion equation requires a CFL-limited time step $\Delta t \leq 0.95\, h^2/(4D)$ for numerical stability, where $h$ is the spatial grid spacing and $D$ is the diffusion coefficient. The function \texttt{interp1} is used to resample the fine-resolution carrier dynamics onto the coarser output grid before passing the current density arrays to the far-field computation. This reduces the memory footprint and the number of time steps in the radiation integral without sacrificing the accuracy of the carrier transport.\newline
To generate two-dimensional transverse field maps at a fixed propagation distance $z_0$, a separate function \texttt{compute\_THz\_E\_spatial\_2D.m} repeats the computation over a coarse grid of observation points, typically $11 \times 11$ points spanning $\pm 50~\mu$m. This function follows the same three-stage algorithm but evaluates the field at a single snapshot in time, the time of peak $|\mathbf{E}|$, rather than producing a full waveform, trading temporal coverage for spatial resolution.

\subsection{MATLAB GUI Interface Design}
The numerical engine described in Section~5.2 produces large multidimensional arrays of carrier density and current data, but extracting physical insight from these arrays through command-line scripting alone is cumbersome and error-prone. To address this, an integrated graphical user interface was developed in MATLAB App Designer to act as a front-end for the diffusion-recombination solver and the far-field radiation module. The motivation behind this design choice was twofold: first, App Designer offers native interoperability with the MATLAB workspace, allowing the same numerical routines used in standalone scripts to be invoked without intermediate data serialisation; second, its event-driven architecture supports the kind of interactive parameter exploration that is essential when investigating how material properties influence the radiated waveform. Alternative frameworks such as PyQt or web-based dashboards were considered during the planning stage but were ultimately rejected because they would have introduced an additional inter-process communication layer between the visualisation front-end and the MATLAB simulation kernel.

\subsubsection{Layout and Functional Organisation}
The interface adopts a tabbed, panel-based layout that separates parameter entry from visualisation, as shown in Figure~\ref{fig:gui_complete}. The upper half of the application window contains the input region on the left and a three-dimensional geometry preview on the right, while the lower half is reserved for the four output panels that display simulation results. Input parameters are grouped into four tabs according to their physical role, which prevents the visual clutter associated with single-page forms and guides the user through the configuration sequence in a logical order.

The first tab, \textit{Mechanism Selector}, exposes a radio-button group with three mutually exclusive options: \textit{Built-In Field}, \textit{Photo-Dember Effect}, and \textit{Custom}. Selecting the first or second option routes execution to the standard drift-diffusion engine with the corresponding physics toggle activated, whereas the \textit{Custom} option reveals three additional text-area fields for user-defined expressions of the current density components $J_x$, $J_y$, and $J_z$. A read-only reference panel listing the allowed variables ($n$, $\partial n/\partial x$, $\partial n/\partial y$, $X$, $Y$, $t$) appears alongside these fields, providing the user with the symbolic vocabulary required to construct valid formulas. The remaining tabs collect numerical material parameters (mobility, recombination lifetime, absorption depth, and lattice temperature), optical excitation parameters (pulse FWHM, fluence, spot size, incidence angle, and pulse shape), and the three Cartesian coordinates of the far-field observation point.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.85\linewidth]{interim_report_latex_template (1)/figure/shenglong/parameter.png}
    \caption{
        Tabbed parameter entry region of the GUI. The Mechanism Selector tab is shown with three exclusive options for the photocurrent source. Selecting Custom reveals additional text fields for user-defined $J_x$, $J_y$, and $J_z$ expressions together with a list of allowed variables.
    }
    \label{fig:gui_param}
\end{figure}

\subsubsection{Parameter Marshalling and Validation}
A central design challenge was to translate user-facing inputs, which are typically expressed in engineering units that physicists find intuitive, into the numerical form required by the simulation engine. The callback associated with the \textit{Update Geometry} push button performs this translation inside a dedicated private method, \texttt{runDiffusionEngine}, that constructs a parameter struct from the GUI fields. Several non-trivial conversions are carried out at this stage. The pulse full-width-at-half-maximum entered by the user is rescaled to the Gaussian standard-deviation form $\tau_p = \mathrm{FWHM}/\sqrt{2\ln 2}$ that the analytical pulse model expects. The fluence value, supplied in J/cm$^2$, is converted to peak intensity through
\begin{equation}
    I_0 = \frac{F\,\cos\theta}{4\,\tau_p},
\end{equation}
where the cosine factor accounts for the geometric reduction in fluence under oblique incidence. Diffusion coefficients for electrons and holes are derived from the user-supplied mobility via the Einstein relation $D = \mu k_B T / q$, with the hole mobility approximated as one tenth of the electron value to reflect the typical asymmetry observed in III-V semiconductors~\cite{Reklaitis2010}. The absorption coefficient is obtained as the reciprocal of the absorption depth, with a lower bound of $10^{-9}$~m to prevent numerical overflow.

Input validation is performed before the parameter struct is dispatched to the solver. Non-positive values for the recombination lifetime or absorption depth raise a modal alert via \texttt{uialert}, and the callback returns without invoking the engine. This prevents the simulation from entering an undefined state and provides the user with immediate, contextual feedback. Stale results from previous runs are explicitly cleared at the start of each callback execution by setting the \texttt{SimResults} property to an empty array, which guarantees that downstream plotting routines never display data inconsistent with the current parameter set.

\subsubsection{Custom Mechanism Parsing}
The \textit{Custom} mechanism mode required a more elaborate parsing pipeline because user-supplied formulas must be compiled into MATLAB function handles at runtime. Within the \texttt{runCustomEngine} method, the contents of the three text-area fields are first concatenated using \texttt{strjoin} to flatten any line breaks introduced by multi-line entry. Empty entries default to the string \texttt{'0'} so that an unspecified component contributes nothing to the radiated field rather than raising a syntax error. The resulting expressions are wrapped in anonymous function syntax through \texttt{str2func}:
\begin{verbatim}
    hx = str2func(['@(n,dnx,dny,X,Y,t) ' fx]);
\end{verbatim}
A wrapper handle is then constructed that evaluates all three components in a single call and returns them via \texttt{deal}, ensuring that the physics engine receives a uniformly typed callable. To detect dimensional inconsistencies before they propagate into the time-stepping loop, the wrapper is invoked on a small test grid of size $3 \times 4$ and the output dimensions are compared to those of the input through \texttt{isequal}. Any mismatch triggers an explanatory alert, which is particularly useful when a user inadvertently writes a formula that collapses an array dimension, for example by using scalar division where element-wise division was intended. A \texttt{try/catch} block surrounds the entire compilation step to convert MATLAB's raw error messages into user-readable dialog boxes.

\subsubsection{Geometry Visualisation and Coordinated Plotting}
The right-hand panel of the upper region renders a three-dimensional schematic of the semiconductor slab and the incident laser beam, drawn through two private methods, \texttt{plotSample} and \texttt{plotBeam}. The semiconductor is represented as a rectangular cuboid by enumerating its six faces and rendering each through MATLAB's \texttt{patch} function with semi-transparent shading. The laser beam is generated using \texttt{cylinder} and rotated about the $y$-axis by the user-specified incidence angle through a standard rotation matrix:
\begin{equation}
    \begin{pmatrix} X' \\ Z' \end{pmatrix}
    =
    \begin{pmatrix} \cos\theta & \sin\theta \\ -\sin\theta & \cos\theta \end{pmatrix}
    \begin{pmatrix} X \\ Z \end{pmatrix}.
\end{equation}
This rotation is applied to the cylindrical surface coordinates before rendering, so that changing the incidence angle in the \textit{Excitation} tab and pressing \textit{Update Geometry} immediately produces an updated visual representation. The geometry view serves both as a sanity check that the entered parameters describe a physically meaningful configuration and as an aid when explaining the experimental setup to a non-specialist audience.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.85\linewidth]{interim_report_latex_template (1)/figure/shenglong/3d.png}
    \caption{
        Three-dimensional schematic produced by the geometry preview panel. The translucent slab represents the semiconductor surface, and the orange cylinder represents the incident laser beam after rotation about the $y$-axis by the configured incidence angle.
    }
    \label{fig:gui_geometry}
\end{figure}

The lower region of the application contains four output displays that are populated in sequence after the simulation engine returns. The \texttt{plotTransientCurrentGUI} method renders the central carrier density $n(0,0,t)$ as a temporal trace, which serves as a one-dimensional proxy for the integrated photocurrent. The \texttt{plotCarrierDistributionGUI} method displays the spatial distribution $n(x,y,t^*)$ at the time $t^*$ of peak central density, using \texttt{imagesc} with the $y$-axis direction reversed to match the physical convention. The \texttt{plotTHzFieldGUI} method populates a tabbed group with three separate axes for the $E_x$, $E_y$, and $E_z$ components of the radiated field at the user-specified observation point. Finally, \texttt{plotSpatialRadiationGUI} draws a quiver plot of the transverse field vectors $(E_x, E_y)$ on the observation plane using the coarse $11 \times 11$ grid produced by the \texttt{compute\_THz\_E\_spatial\_2D} routine, allowing the user to inspect the polarisation structure described in Section~5.2.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.85\linewidth]{interim_report_latex_template (1)/figure/shenglong/complete.png}
    \caption{
        Complete GUI overview after a successful simulation run. The upper region shows the parameter tabs and the geometry preview. The lower region displays the central carrier density trace, the carrier distribution at peak time, and the radiated field at the chosen observation point.
    }
    \label{fig:gui_complete}
\end{figure}

\subsubsection{Performance Considerations}
Coupling a computationally expensive solver to a single-threaded GUI raised concerns about responsiveness, particularly during the far-field summation, which dominates total runtime. Two design choices were made to mitigate this. First, the standard mechanism path uses the high-resolution spatial grid of $601 \times 601$ points required for accurate field reconstruction, while the \textit{Custom} path operates on a reduced $201 \times 201$ grid, recognising that user-defined formulas are typically used for exploratory work where rapid feedback matters more than fine spatial resolution. Second, the spatial radiation map is restricted to an $11 \times 11$ observation grid, which limits the additional cost relative to a single-point waveform calculation while still revealing the radial polarisation pattern. With these settings, a complete simulation cycle from button press to fully rendered output typically completes within approximately one minute on a standard desktop machine, which was judged acceptable for an interactive exploratory tool.

The resulting interface forms a complete front-end to the photocurrent simulation pipeline, mapping user-defined physical parameters onto the diffusion solver, the far-field integrator, and the spatial field-mapping routine in a single coordinated workflow. A blank instance of the application is shown in Figure~\ref{fig:gui_blank} for reference; populated outputs corresponding to the default parameter set appear in Figure~\ref{fig:gui_complete}.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.85\linewidth]{interim_report_latex_template (1)/figure/shenglong/blank.png}
    \caption{
        Initial state of the GUI before any simulation has been executed. All output axes display empty default coordinate frames, and the geometry preview awaits the first invocation of the \textit{Update Geometry} callback.
    }
    \label{fig:gui_blank}
\end{figure}
%% ============================================================
%% RESULTS AND DISCUSSION
%% ============================================================
\section{Results}

\subsection{Photo-Dember Model Results}
The Photo-Dember effect arises from the ultrafast lateral diffusion of photo-generated carriers following femtosecond laser excitation. Because electrons have a significantly higher mobility than holes, they diffuse outward faster, creating a transient charge separation and a net radial photocurrent $\mathbf{J} = q(D_e - D_h)\nabla n$ that radiates THz electromagnetic waves. The simulation results presented in this section validate the Photo-Dember transport model by comparing the angular emission pattern with published experimental data.

\subsubsection{Angular Emission Pattern and Experimental Validation}
To validate the simulated far-field radiation pattern, the angular response was compared with the experimental data presented in Fig.~8(b) of Mueckstein et al.~\cite{Mueckstein2015}, who measured the peak-to-peak THz amplitude emitted from Fe:InGaAs surfaces excited at normal incidence with a 250~$\mu$m-diameter beam at 800~nm. The simulation was run in Photo-Dember mode, since Mueckstein et al.\ demonstrated that the lateral in-plane photocurrents are the dominant THz generation mechanism in Fe:InGaAs, with the built-in field contribution being negligible due to the small band-bending in this narrow-bandgap material. The material parameters were chosen to match the experimental conditions: electron mobility $\mu_e = 0.5$~m$^2$/Vs, hole mobility $\mu_h = 0.02$~m$^2$/Vs, carrier lifetime $\tau_r = 300$~fs, absorption coefficient $\alpha = 10^6$~m$^{-1}$ at 800~nm, and beam waist $w_0 = 106~\mu$m corresponding to a FWHM spot diameter of approximately 250~$\mu$m.

The lateral observation position was fixed at $X_0 = 3.2$~mm while the vertical position $Z_0$ was varied to sweep the observation angle
\begin{equation}
    \theta_1 = \tan^{-1}\!\left(\frac{X_0}{Z_0}\right),
\end{equation}
measured from the surface normal. The corresponding source-to-observer distance for each configuration is $R = \sqrt{X_0^2 + Z_0^2}$. At each angle, the full time-domain waveform was computed using \texttt{compute\_THz\_E\_from\_Jx}, and the $E_z$ component was selected as the detector-like signal because, for an observation point in the $xz$-plane, the $z$-component captures the projection of the radially polarised far-field onto the vertical axis, which varies most strongly with emission angle. The peak-to-peak amplitude was then extracted as
\begin{equation}
    E_{\mathrm{pp}} = \max\bigl(E_z(t)\bigr) - \min\bigl(E_z(t)\bigr),
\end{equation}
which captures the full bipolar swing of the transient waveform rather than only the positive peak.

Because $X_0$ was held constant while $Z_0$ was varied, the source-to-observer distance $R$ changes with angle. In the far-field regime, the radiation amplitude decays as $1/R$, so a distance correction was applied to isolate the intrinsic angular dependence of the source:
\begin{equation}
    E_{\mathrm{pp,corr}} = E_{\mathrm{pp}} \times R.
\end{equation}
The corrected values were then normalised by their maximum:
\begin{equation}
    E_{\mathrm{pp,norm}} = \frac{E_{\mathrm{pp,corr}}}{\max(E_{\mathrm{pp,corr}})}.
\end{equation}

Table~\ref{tab:angular} lists the distance-corrected and normalised peak-to-peak amplitudes for 19 observation angles spanning $18^\circ$ to $90^\circ$. The resulting angular emission pattern is plotted in Figure~\ref{fig:angular_sim}.

\begin{table}[h]
\centering
\caption{Distance-corrected and normalised far-field peak-to-peak amplitude $E_{\mathrm{pp}}$ using the $E_z$ component. The maximum corrected amplitude occurs at $\theta_1 = 25^\circ$.}
\label{tab:angular}
\small
\begin{tabular}{r r r r r r}
\toprule
$\theta_1$ (deg) & $E_{\mathrm{pp,corr}}$ & $E_{\mathrm{pp,norm}}$
&
$\theta_1$ (deg) & $E_{\mathrm{pp,corr}}$ & $E_{\mathrm{pp,norm}}$ \\
\midrule
18 & 944579.21 & 0.960 & 50 & 709373.71 & 0.721 \\
20 & 954827.59 & 0.970 & 55 & 632516.39 & 0.643 \\
22 & 969064.67 & 0.985 & 60 & 548824.05 & 0.558 \\
25 & 984320.25 & 1.000 & 65 & 461861.39 & 0.469 \\
28 & 975306.37 & 0.991 & 70 & 372318.87 & 0.378 \\
30 & 958110.40 & 0.973 & 75 & 280706.05 & 0.285 \\
35 & 914776.02 & 0.929 & 80 & 188521.10 & 0.192 \\
40 & 853777.96 & 0.867 & 85 & 94227.23  & 0.096 \\
45 & 780287.46 & 0.793 & 90 & 86.00     & 0.000 \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[b]
    \centering
    \includegraphics[width=0.6\linewidth]{interim_report_latex_template (1)/figure/yaoqqi/different_observation_angles.png}
    \caption{Simulated time-domain THz waveforms detected at selected observation angles. The $E_z$ component was used as the detector-like signal.
    }
    \label{fig:pulse_detected}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.55\linewidth]{interim_report_latex_template (1)/figure/yaoqqi/result1.png}
    \caption{
        Simulated far-field angular emission pattern. The normalised peak-to-peak amplitude $E_{\mathrm{pp,norm}}$ is plotted as a function of the emission angle $\theta_1$ from the surface normal.
    }
    \label{fig:angular_sim}
\end{figure}

The simulated pattern exhibits the characteristic features of in-plane dipole radiation: negligible emission near the surface normal, a broad maximum centred at $\theta_1 \approx 25^\circ$, and a monotonic decrease toward incidence. These features are in close qualitative agreement with the experimental data of Mueckstein et al.~\cite{Mueckstein2015}, where the measured emission from Fe:InGaAs peaks in the range $20^\circ$--$30^\circ$ and vanishes along the surface normal.

The null at $\theta_1 = 0^\circ$ arises because the radially symmetric photo-Dember source produces equal and opposite transverse field contributions from diametrically opposite source cells, which cancel exactly along the symmetry axis, as predicted by the double cross product in Equation~(\ref{eq:farfield}). The broad peak at intermediate angles reflects the trade-off between two competing geometric effects: at small angles the transverse projection of $\dot{\mathbf{J}}$ is weak, while at large angles the oblique path through the source increases $R$ and reduces the solid angle subtended by the detector.

The simulated peak angle of $25^\circ$ falls within the experimentally observed range of $20^\circ$--$25^\circ$, demonstrating good quantitative agreement. Minor residual differences in the detailed shape of the pattern may arise from several factors: the simulation employs a drift-diffusion model that does not capture ballistic carrier transport in the first few hundred femtoseconds, the model does not account for refraction at the semiconductor--air interface, and the Fe:InGaAs material parameters are approximate values from the literature rather than measured values for the specific epilayer used by Mueckstein et al. Despite these simplifications, the close agreement in both the peak angle and the overall shape of the angular pattern validates the retarded-potential framework combined with the Photo-Dember source model as a physically meaningful description of far-field THz emission from semiconductor surfaces.

\subsection{Built-in Field Model Results}
The built-in field mechanism generates THz radiation through the vertical drift of photo-generated carriers in the surface depletion region. In wide-bandgap semiconductors such as GaAs, Fermi-level pinning at the surface creates a static depletion field that accelerates photo-generated electrons and holes in opposite vertical directions, producing a transient surge current $J_z(t) \propto n(t)$ proportional to the instantaneous carrier density~\cite{SimulationTHz}. Unlike the Photo-Dember effect, which produces lateral currents from carrier density gradients, the built-in field drives a purely vertical current whose spatial profile follows the Gaussian optical excitation envelope. The key prediction of this model is that the radiated THz pulse should exhibit a \textit{bipolar} temporal waveform, as demonstrated below.

\subsubsection{Bipolar Pulse Waveform}
The far-field radiation from any transient current source is proportional to $\dot{\mathbf{J}}(t_{\mathrm{ret}})$, the time derivative of the current density evaluated at the retarded time (Equation~\ref{eq:farfield}). For the built-in field mechanism, the vertical surge current $J_z(t)$ follows the temporal profile of the carrier density: it rises rapidly during the femtosecond laser excitation as carriers are photo-generated and accelerated by the depletion field, then decays on a longer timescale as the carriers recombine and the depletion field is screened by charge separation. The time derivative $\dot{J}_z(t)$ is therefore positive during the rising phase and negative during the falling phase, producing a bipolar radiated pulse with a leading positive lobe followed by a trailing negative lobe. This characteristic waveform was first predicted by the Hertzian dipole model of Auston et al.~\cite{auston_picosecond_1984} and has since been confirmed in both Monte Carlo simulations of GaAs surface emission~\cite{SimulationTHz} and experimental THz time-domain measurements~\cite{Reklaitis2010}.

Figure~\ref{fig:ExEy} presents the simulated $E_x$ and $E_y$ waveforms computed in Built-In Field mode. The observation point $(3.2~\mathrm{mm},\, -3.2~\mathrm{mm},\, -6.0~\mathrm{mm})$ was chosen to be off-axis in both $x$ and $y$ so that both transverse field components are non-zero, and on the substrate side ($z < 0$) to represent the common experimental geometry in which THz radiation is collected through the substrate. Both components display the expected bipolar profile, but with opposite leading polarities: $E_x$ is positive-first while $E_y$ is negative-first. This sign difference arises from the geometric projection of the vertical current derivative $\dot{J}_z$ onto the transverse plane via the double cross product in Equation~(\ref{eq:farfield}). Since the observer is displaced in the positive $x$ direction, the projection onto $E_x$ acquires one sign, whereas the negative $y$ displacement produces the opposite sign in $E_y$. The bipolar shape itself — a sharp leading lobe followed by a broader trailing lobe of opposite polarity — is a direct consequence of the temporal asymmetry in the surge current $J_z(t)$. The leading lobe corresponds to the rapid onset of the photocurrent. The trailing lobe corresponds to the slower decay as carriers recombine with lifetime $\tau_r$, which is typically one to two orders of magnitude longer than the pulse duration. This asymmetric bipolar waveform is a well-established signature of surface-emitted THz pulses from semiconductors with a strong depletion field~\cite{SimulationTHz,auston_picosecond_1984}.

Both components return to zero well within the 45~ps simulation window ($T_{\min} = -5$~ps to $T_{\max} = 40$~ps), confirming that the full transient is captured without truncation artefacts. The successful reproduction of the bipolar waveform, including the correct lobe asymmetry and temporal width, validates the built-in field implementation within the simulation framework.

\begin{figure}[t]
	\centering
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{interim_report_latex_template (1)/figure/yaoqqi/Ex.png}
		\caption{$E_x(t)$}
		\label{fig:Ex}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{interim_report_latex_template (1)/figure/yaoqqi/Ey.png}
		\caption{$E_y(t)$}
		\label{fig:Ey}
	\end{subfigure}
	\caption{Temporal waveforms of (a)~$E_x(t)$ and (b)~$E_y(t)$ computed in Built-In Field mode at the observation point $(3.2~\mathrm{mm},\, -3.2~\mathrm{mm},\, -6.0~\mathrm{mm})$, on the substrate (transmitted) side. Both components exhibit the bipolar pulse shape predicted by the Hertzian dipole model~\cite{auston_picosecond_1984}: a sharp positive lobe from the rising surge current followed by a broader negative lobe from the decay phase.}
	\label{fig:ExEy}
\end{figure}

\subsection{GUI Interface Results}

\subsubsection{Custom Mechanism Validation}
The Custom mechanism mode allows users to define arbitrary current density expressions $J_x(n, \partial n/\partial x, \partial n/\partial y, x, y, t)$, $J_y(\cdots)$, and $J_z(\cdots)$ as MATLAB formulas that are parsed and compiled at runtime. To validate that this pipeline produces physically correct results, the Custom mode was used to reproduce the Built-In Field mechanism, and the output was compared against the standard Built-In mode.

In the Built-In Field model, the THz-generating current arises from the drift of photo-generated carriers in the surface depletion field. The depletion region extends to a depth $W_{\mathrm{dep}}$ below the semiconductor surface, within which a static electric field $E_z(z)$ accelerates electrons and holes in opposite vertical directions. The resulting net vertical current density is
\begin{equation}
    J_z(x,y,t) = q\,\mu_e\,E_z(z)\,n(x,y,t),
    \label{eq:Jz_builtin}
\end{equation}
where $q$ is the elementary charge, $\mu_e$ is the electron mobility, and $n(x,y,t)$ is the carrier density at the surface. Crucially, $J_z$ is proportional to $n$: the spatial profile follows the Gaussian optical excitation envelope, and the temporal evolution follows the carrier generation and recombination dynamics. There is no lateral current in this mechanism, so $J_x = J_y = 0$.

This proportionality means that the Built-In Field can be reproduced in Custom mode by entering:
\begin{center}
\texttt{Jx = 0}, \quad \texttt{Jy = 0}, \quad \texttt{Jz = -n}
\end{center}
The expression \texttt{Jz = -n} captures the essential physics: the vertical current is proportional to the local carrier density and directed downward (negative $z$) because electrons drift against the depletion field. The physical prefactors $q$, $\mu_e$, and $E_z$ are omitted, so the Custom output is expected to differ from the Built-In output by a constant scale factor while preserving the identical waveform shape.

Figure~\ref{fig:custom_validation} compares the $E_x$ component produced by the two modes at the same observation point. The temporal waveforms are identical in shape: both exhibit the same bipolar pulse structure with the same zero-crossing time and the same ratio of positive to negative lobes. The only difference is the amplitude scale, which differs by a factor of approximately $10^{19}$. This factor is consistent with the missing physical constants: $q \cdot \mu_e \cdot E_{\mathrm{max}} \approx 1.6 \times 10^{-19} \times 0.8 \times 8 \times 10^{5} \approx 10^{-13}$~A/m, combined with the depth integration over the depletion region. The exact agreement in waveform shape confirms that the Custom formula parser, the runtime compilation via \texttt{str2func}, and the subsequent far-field integration all operate correctly.

\begin{figure}[t]
	\centering
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{interim_report_latex_template (1)/figure/yaoqqi/Ex2.png}
		\caption{Custom mode: \texttt{Jz = -n}}
		\label{fig:custom_builtin}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{interim_report_latex_template (1)/figure/yaoqqi/Ex22.png}
		\caption{Built-In Field mode}
		\label{fig:standard_builtin}
	\end{subfigure}
	\caption{Comparison of the $E_x$ waveform produced by (a)~the Custom mechanism with \texttt{Jz~=~-n} and (b)~the standard Built-In Field mode. The waveform shapes are identical; the amplitude difference ($\times 10^{23}$ vs $\times 10^{4}$) reflects the omitted physical prefactors $q\mu_e E_z$ in the Custom expression.}
	\label{fig:custom_validation}
\end{figure}

An analogous test was performed for the Photo-Dember mechanism. In this case, the THz-generating current arises from the lateral diffusion of photo-generated carriers. Because electrons and holes have different mobilities, they diffuse at different rates, producing a net lateral current proportional to the spatial gradient of the carrier density. The net Photo-Dember current for the $x$-component is
\begin{equation}
    J_x(x,y,t) = q(D_e - D_h)\frac{\partial n}{\partial x},
    \label{eq:Jx_dember}
\end{equation}
where $D_e$ and $D_h$ are the electron and hole diffusion coefficients respectively. The positive sign arises because the electron charge is negative: the electron particle flux is $\boldsymbol{\Gamma}_e = -D_e \nabla n$ (down the gradient), but the corresponding current is $\mathbf{J}_e = -q\boldsymbol{\Gamma}_e = qD_e \nabla n$ (opposite to the particle flow). The hole current $\mathbf{J}_h = -qD_h \nabla p$ partially cancels the electron contribution, leaving a net current proportional to $(D_e - D_h)\nabla n$. An identical expression applies to $J_y$ with $\partial n/\partial y$, while $J_z = 0$ since the Photo-Dember effect produces no vertical current.

This proportionality to the carrier density gradient means that the Photo-Dember mode can be reproduced in Custom mode by entering:
\begin{center}
\texttt{Jx = dnx}, \quad \texttt{Jy = dny}, \quad \texttt{Jz = 0}
\end{center}
where \texttt{dnx} and \texttt{dny} are the spatial gradients $\partial n/\partial x$ and $\partial n/\partial y$ provided by the simulation engine. The physical prefactors $q$ and $(D_e - D_h)$ are omitted, so the output amplitude will differ by a corresponding scale factor.

Figure~\ref{fig:custom_dember} compares the $E_x$ waveform from the two modes. The main bipolar pulse shape is reproduced faithfully: the same zero-crossing time, lobe ratio, and temporal width. The amplitude differs by a factor of approximately $10^{20}$, consistent with the missing prefactor $q(D_e - D_h) \approx 1.6 \times 10^{-19} \times (0.021 - 0.0026) \approx 3 \times 10^{-21}$~C\,m$^2$/s.

A minor difference is visible in the trailing positive feature at $t \approx 28.5$~ps: the bump is present in both waveforms but is more pronounced in the Custom output. This arises from a fundamental difference in how the two modes represent the lateral current. In the standard Photo-Dember mode, the spatial current pattern is factorised as
\begin{equation}
    J_x(x,y,t) = q\,D_{\mathrm{eff}}\, n_{\mathrm{surf}}(t) \cdot \frac{\partial G}{\partial x},
\end{equation}
where $\partial G/\partial x$ is the fixed spatial gradient of the Gaussian excitation envelope and $n_{\mathrm{surf}}(t)$ is a scalar time function representing the surface carrier density. Under this factorisation, the spatial shape of $J_x$ remains frozen as the initial Gaussian gradient at all times; only the overall amplitude varies. In the Custom mode, however, the expression \texttt{Jx~=~dnx} evaluates the gradient of the full time-evolving carrier density $n(x,y,t)$. As carriers diffuse laterally after excitation, the density profile broadens and the gradient pattern changes in both shape and position at every time step. This evolving spatial structure introduces additional time variation into $\dot{\mathbf{J}}$, which manifests as the enhanced trailing feature in the radiated field. The standard mode's factorised approximation suppresses this effect by construction, so its trailing bump is smaller. In this respect, the Custom mode provides a more physically complete representation of the diffusion dynamics, albeit at the cost of the amplitude calibration provided by the built-in prefactors.

Together with the Built-In Field validation above, this confirms that the Custom mechanism parser correctly handles both lateral gradient-driven and vertical drift-driven current expressions, and that the far-field integration pipeline produces physically consistent results regardless of the input pathway.

\begin{figure}[t]
	\centering
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{interim_report_latex_template (1)/figure/yaoqqi/Photodember.png}
		\caption{Photo-Dember mode}
		\label{fig:standard_dember}
	\end{subfigure}
	\hfill
	\begin{subfigure}[t]{0.48\textwidth}
		\centering
		\includegraphics[width=\textwidth]{interim_report_latex_template (1)/figure/yaoqqi/customPhotodember.png}
		\caption{Custom mode: \texttt{Jx = dnx, Jy = dny}}
		\label{fig:custom_dember_sub}
	\end{subfigure}
	\caption{Comparison of the $E_x$ waveform produced by (a)~the standard Photo-Dember mode and (b)~the Custom mechanism with \texttt{Jx~=~dnx}, \texttt{Jy~=~dny}. The waveform shapes are identical; the amplitude difference ($\times 10^{8}$ vs $\times 10^{28}$) reflects the omitted prefactor $q(D_e - D_h)$.}
	\label{fig:custom_dember}
\end{figure}

\section{Discussion}

\subsection{Model Assumptions and Limitations}
The simulation framework relies on several simplifying assumptions whose impact on the results should be considered. The most fundamental is the use of drift-diffusion transport, in which the electron drift velocity is computed as $v_n = -\mu_e E_z$ and the diffusion flux as $-D_e\,\partial n/\partial z$, assuming that carriers instantaneously reach a steady-state velocity set by the local electric field. In reality, after femtosecond excitation, photo-generated carriers in III-V semiconductors undergo a brief ballistic phase lasting approximately 100--300~fs before enough scattering events occur to establish the steady-state drift regime~\cite{Lu2022}. During this phase, carriers can temporarily exceed the drift-diffusion velocity, an effect known as velocity overshoot~\cite{Malevich2021}. The Monte Carlo approach used by Johnston et al.~\cite{SimulationTHz} tracks individual carriers through discrete scattering events and naturally captures this ballistic regime. Since the present model assumes steady-state transport from $t = 0$, it may slightly underestimate the initial rate of change of the photocurrent $\dot{J}$, and hence the high-frequency content of the radiated THz pulse. However, the majority of the pulse shape is determined by the slower picosecond-scale carrier recombination, which is well-described by the drift-diffusion model, and the close agreement between the simulated bipolar waveform (Section~4.2) and the established literature~\cite{SimulationTHz,auston_picosecond_1984} confirms that this limitation does not significantly affect the overall waveform. A second simplification is the treatment of optical absorption: the carrier generation rate uses a fixed absorption coefficient $\alpha$ following Beer's law, with no dependence on the instantaneous carrier density. At high excitation fluences, band-filling can reduce the effective absorption as states near the band edge become occupied, and free-carrier absorption can increase the total absorption~\cite{Pettine2023}. These nonlinear effects are not included, but at the moderate fluence used in the simulations ($F = 0.1$~mJ/cm$^2$) the photo-generated carrier density remains well below the level at which band-filling becomes significant, so this simplification is expected to have a negligible impact. The far-field computation also assumes free-space propagation from the source plane to the observation point, with no refractive index, Fresnel transmission coefficient, or Snell refraction applied at the semiconductor--air interface. In practice, III-V semiconductors have a refractive index of approximately 3.5--3.7 at THz frequencies~\cite{Wu2024}, which would cause partial reflection and angular deflection of the outgoing radiation. Despite this omission, the simulated peak emission angle of $25^\circ$ (Section~4.1) falls within the experimentally measured range of $20^\circ$--$25^\circ$ reported by Mueckstein et al.~\cite{Mueckstein2015}, indicating that the intrinsic angular dependence of the Photo-Dember source dominates the pattern shape and that refraction introduces a quantitative correction rather than a qualitative change. Finally, the depth-resolved transport solver uses a forward Euler scheme with a time step of $\Delta t = 0.5$~fs, constrained by the CFL stability condition $\Delta t \leq 0.95\,h^2/(4D)$ for the spatial grid spacing $h$ and diffusion coefficient $D$. This requires approximately $9 \times 10^7$ internal iterations for the default 45~ps simulation window; an implicit or semi-implicit scheme would relax the time-step constraint and potentially reduce computation time, though the current runtime of approximately one minute per observation point was judged acceptable for an interactive GUI tool.

\subsection{Photo-Dember versus Built-in Field Mechanisms}
The results in Section~4 demonstrate that both the Photo-Dember and Built-in Field mechanisms produce physically valid THz radiation within the same simulation framework, but their relative contributions depend strongly on the material system and surface conditions. In narrow-bandgap semiconductors such as Fe:InGaAs, where the surface band-bending is small and the mobility asymmetry between electrons and holes is large, the lateral Photo-Dember current is the dominant source of THz emission~\cite{Klatt2010}. This is consistent with the findings of Mueckstein et al.~\cite{Mueckstein2015}, who confirmed experimentally that the radially polarised emission pattern from Fe:InGaAs is characteristic of in-plane photocurrents rather than vertical drift currents. The angular emission validation in Section~4.1, which used Fe:InGaAs parameters in Photo-Dember mode, supports this conclusion.

In contrast, wide-bandgap semiconductors such as GaAs and InP typically exhibit strong Fermi-level pinning at the surface, creating a depletion region with a built-in electric field of order $10^5$--$10^6$~V/m~\cite{SimulationTHz}. In these materials, the vertical surge current driven by the depletion field dominates THz generation, producing the linearly polarised emission that is observed experimentally~\cite{auston_picosecond_1984}. The bipolar pulse waveform presented in Section~4.2, computed with \texttt{useBuiltIn~=~true} and \texttt{usePhotoDember~=~false}, reproduces this behaviour with the correct lobe asymmetry determined by the ratio of the laser pulse duration to the carrier recombination lifetime. Recent studies have further confirmed that surface-field-driven THz emission provides a sensitive probe of depletion-layer properties in semiconductor surfaces~\cite{YangTonouchi2022}.

The GUI presents these two mechanisms as mutually exclusive selections, reflecting the fact that each targets a different class of semiconductor material. However, the Custom mechanism mode provides a route to combine both effects: a user could enter a current expression that includes both a vertical drift term proportional to $n$ and a lateral diffusion term proportional to $\nabla n$, effectively superimposing the two sources. This flexibility allows investigation of materials where both mechanisms contribute comparably, without requiring changes to the simulation engine. More broadly, the Custom mode enables the exploration of novel THz generation mechanisms beyond the two standard models by accepting arbitrary current density expressions as input~\cite{Kefayati2024}.

%% ============================================================
%% CONCLUSIONS
%% ============================================================
\section{Conclusions}
% TODO: Write conclusions that summarise the main findings.
% - What is your main project result?
% - What did you achieve?
% - What lessons were learned?
% This replaces the old "Project management" and "Future work" sections.

%% ============================================================
%% TEAM CONTRIBUTIONS
%% ============================================================
\section{Team Contributions}
% TODO: List team member contributions and indicate who wrote which sections.
%
% Yaoqi Dong -- Photo-Dember effect modelling (1D, 2D, dual-carrier)
% Yuhao Chang -- Electric field computation (retarded potentials, 2D/3D field simulation)
% Shenglong Li -- MATLAB GUI design and integration

\newpage
\bibliography{example-references}
\bibliographystyle{IEEEtran} % IEEE style



\end{document}
