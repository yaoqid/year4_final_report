\documentclass[12pt]{interim_report}
\usepackage{amsmath,amsthm,graphicx}

\title{Modelling terahertz pulse generation due to ultrafast photocurrents in semiconductors }
\author{Yaoqi Dong, Yuhao Chang, Shenglong Li}
\date{12 December 2025}
\supervisor{Professor Oleg Mitrofanov }
\aiuse{{\bf If you have not used generative AI make sure that {\tt\bf\textbackslash aiuse} is empty. Only edit and include text inside {\tt\bf\textbackslash aiuse} if you have used generative AI.}

I acknowledge the use of name and version of generative AI system  Chat-GPT-  (Publisher OpenAI, URL of the AI system  \\https://chat.openai.com) to brief description of context in which AI tool was used to summarise my initial notes, help with LaTeX format and proofread my final draft.}
\wordcount{4401}

\begin{document}

\coverpage
\declaration
\maketitle

\tableofcontents

%% ============================================================
%% EXECUTIVE SUMMARY
%% ============================================================
\section{Executive Summary}
% TODO: Write a concise executive summary (no more than one page).
% - Briefly what is this project about?
% - Discuss the main points -- one paragraph each
% - Include the main results and findings
% - Spark the reader's interest!

%% ============================================================
%% INTRODUCTION AND LITERATURE REVIEW
%% ============================================================
\section{Introduction and Literature Review}

\subsection{Introduction}
The generation of terahertz radiation via the excitation of semiconductor surfaces with femtosecond laser pulses is a fundamental technique in modern photonics~\cite{Pettine2023}. This process relies on the acceleration of photo-generated carriers, where the resulting transient current acts as a Hertzian dipole source radiating electromagnetic waves into free space. While the physical mechanisms governing these currents—such as the Photo-Dember effect and surface depletion fields—are well-established in theoretical literature, the complex electrodynamics governing the subsequent propagation of the electric field often remain abstract and difficult to visualise.
\newline
The primary objective of this project is to bridge this gap by developing a dedicated simulation interface using MATLAB App Designer. Unlike standard numerical scripts, this graphical interface provides an interactive environment to model and visualise the generation of THz fields based on the method of Retarded Potentials. The specific motivation is to create a versatile tool that allows users to modify intrinsic semiconductor properties—such as carrier lifetime, diffusion coefficients, and absorption depth—to dynamically simulate the radiation characteristics of different materials. By transforming static theoretical equations into observable temporal waveforms and spatial vector maps, this interface serves as both a pedagogical tool for understanding surface emission physics and a predictive platform for optimising semiconductor materials in terahertz emitter design. Notably, recent experimental studies have demonstrated that THz emission spectroscopy can be used as a rapid, noncontact characterisation method for semiconductor surfaces~\cite{YangTonouchi2022}, underscoring the practical relevance of accurate THz emission models.

\subsection{Literature Review}
% TODO: Expand this into a proper literature review.
% - How is your project informed by existing knowledge?
% - How does existing knowledge relate to your project goals?
% - How will you use existing knowledge in your project?
% - How will you contribute to knowledge?
% Reference existing citations: \cite{Hebling:02}, \cite{SimulationTHz}, \cite{auston_picosecond_1984}

%% ============================================================
%% PROJECT GOALS
%% ============================================================
\section{Project Goals}
% TODO: Clearly state the goals/objectives of the project.
% - What are you trying to achieve?
% - What are the specific deliverables?

%% ============================================================
%% TECHNICAL BACKGROUND
%% ============================================================
\section{Technical Background}

\subsection{Photo-Dember Effect}
% TODO: Move the theoretical explanation of the Photo-Dember effect here.
% Present the background theory, mathematical framework, and physical principles.
The model is based on the diffusion-recombination theory: a highly non-equilibrium carrier distribution is generated in the semiconductor by ultra-short laser pulses and then evolves under the driving force of spatial gradients and mobility differences. Existing literature has pointed out that the time derivative of the instantaneous photogenerated current is the main source of terahertz radiation~\cite{Hebling:02}. Recent comprehensive reviews of ultrafast carrier dynamics in terahertz photoconductors have further highlighted the central role of sub-picosecond transport phenomena in governing THz emission efficiency~\cite{Lu2022}. Therefore, the numerical framework of this project is based on this physical principle.

\subsection{Electric Field Theory}
\subsubsection{From Photocurrent to Electromagnetic Radiation}
The fundamental link between ultrafast photocurrents and terahertz emission lies in classical electrodynamics. When a femtosecond laser pulse excites a semiconductor surface, it generates a transient current density $\mathbf{J}(\mathbf{r}', t)$ through mechanisms such as the Photo-Dember effect and surface depletion field drift. Since this current varies on a sub-picosecond timescale, it radiates electromagnetic energy in the terahertz frequency range~\cite{Yang2023}. This principle was first experimentally verified by Auston et al.~\cite{auston_picosecond_1984}, who demonstrated that the far-field radiation from a transient current source is proportional to the first time derivative of the dipole moment. Recent studies have further shown that similar transient photocurrent mechanisms drive THz emission in semiconductor heterostructures, where ballistic photocurrents across band offsets produce measurable radiation pulses~\cite{Malevich2021}. The theoretical framework employed in this project treats each infinitesimal current element on the semiconductor surface as a Hertzian dipole, and the total radiated field is obtained by superposition, following the approach of Johnston et al.~\cite{SimulationTHz}.

\subsubsection{Retarded Potentials and Jefimenko's Equations}
The electric field at an observation point $\mathbf{r}$ due to a time-varying source distribution is rigorously described by Jefimenko's equations, which are the retarded-time solutions to Maxwell's equations~\cite{griffiths_electrodynamics}. The key physical insight is causality: the field observed at time $t$ depends on the state of the source at the earlier retarded time
\begin{equation}
    t_{\mathrm{ret}} = t - \frac{R}{c},
\end{equation}
where $R = |\mathbf{r} - \mathbf{r}'|$ is the distance from the source point $\mathbf{r}'$ to the observation point $\mathbf{r}$, and $c$ is the speed of light.

The full Jefimenko expression for the electric field contains three terms: a Coulomb (electrostatic) term proportional to $1/R^3$, an induction term proportional to $1/R^2$, and a radiation term proportional to $1/R$. In the far-field regime, where the observation distance is much larger than the spatial extent of the source ($R \gg d$), the $1/R^3$ and $1/R^2$ terms become negligible. The dominant radiative contribution is then
\begin{equation}
    \mathbf{E}(\mathbf{r}, t) \approx \frac{\mu_0}{4\pi} \iint_{S} \frac{\hat{\mathbf{R}} \times \left[\hat{\mathbf{R}} \times \dot{\mathbf{J}}(\mathbf{r}', t_{\mathrm{ret}})\right]}{R} \, dS',
    \label{eq:farfield}
\end{equation}
where $\hat{\mathbf{R}} = (\mathbf{r} - \mathbf{r}')/R$ is the unit vector from source to observer, $\dot{\mathbf{J}} = \partial \mathbf{J}/\partial t$ is the time derivative of the current density, and $\mu_0$ is the permeability of free space. The double cross product $\hat{\mathbf{R}} \times [\hat{\mathbf{R}} \times \dot{\mathbf{J}}]$ extracts only the transverse component of the current acceleration, ensuring that only radiation polarised perpendicular to the line of sight propagates to the far field. This formulation has been successfully applied to simulate terahertz generation at semiconductor surfaces by Johnston et al.~\cite{SimulationTHz}, who used a semiclassical Monte Carlo model to compute carrier dynamics and then evaluated the far-field radiation using the retarded potential framework. More recently, Kefayati and Nikoli\'{c}~\cite{Kefayati2024} demonstrated that Jefimenko's equations can be combined with time-dependent density functional theory to reveal the microscopic origins of terahertz radiation in spintronic emitters, further validating the generality of this approach for modelling ultrafast electromagnetic emission.

\subsubsection{Physical Interpretation}
Equation~(\ref{eq:farfield}) encodes several important physical features:
\begin{itemize}
    \item \textbf{Proportionality to $\dot{\mathbf{J}}$}: The radiated field is driven by the \textit{rate of change} of the current, not the current itself. A steady current does not radiate; only accelerating or decelerating charges produce electromagnetic radiation. This is consistent with the Larmor radiation formula in classical electrodynamics.
    \item \textbf{Retardation}: Each source element contributes to the observed field at a time delayed by $R/c$. For a spatially extended source, different elements contribute at different retarded times, producing temporal broadening and shaping of the observed pulse.
    \item \textbf{Geometric projection}: The double cross product ensures that the observed polarisation depends on the viewing angle. For a radially symmetric current distribution arising from the Photo-Dember diffusion mechanism, the transverse field components on-axis cancel by symmetry, while off-axis observation yields a net radially polarised signal.
    \item \textbf{$1/R$ decay}: The radiation field amplitude decreases as $1/R$, meaning the radiated power (proportional to $|\mathbf{E}|^2$) follows an inverse-square law, as expected for freely propagating waves.
\end{itemize}

In the context of this project, the current density $\mathbf{J}$ is obtained from the carrier transport simulation, which solves the diffusion-recombination equation for photo-generated electrons and holes in the semiconductor. The photocurrent arises from the spatial gradient of the carrier density via $\mathbf{J} = -qD\nabla n$, where $q$ is the elementary charge and $D$ is the diffusion coefficient. The Photo-Dember effect produces a net transient current because electrons diffuse faster than holes due to their higher mobility~\cite{Reklaitis2010}, creating a charge separation that radiates terahertz electromagnetic waves. Recent work by Hale et al.~\cite{Hale2022} has demonstrated that even ultrathin (160~nm) GaAs metasurfaces can generate THz pulses with efficiency comparable to bulk crystals, and Jung et al.~\cite{Jung2024} have further shown that InAs metalens emitters based on Mie resonators can produce focused THz beams with amplitudes up to 20 times greater than plasmonic emitters, highlighting how charge density gradients at nanostructured surfaces serve as effective radiation sources through similar photocurrent mechanisms.

%% ============================================================
%% METHODS
%% ============================================================
\section{Methods}

\subsection{Photo-Dember Numerical Model}
First, a one-dimensional electron diffusion-composition model was constructed. By applying Gaussian excitation in both space and time, the expected carrier behavior was successfully simulated: rapid accumulation at the excitation center, symmetric diffusion over time, and exponential decay. The model output was consistent with the analytical expectations, verifying the stability and physical correctness of the numerical method.\newline
Subsequently, the model was extended to two dimensions to reflect the circular symmetry of the real light spot. The two-dimensional results showed that the carriers diffused radially symmetrically and an electron density peak slightly delayed relative to the excitation peak appeared, which was consistent with the ultrafast carrier dynamics reported in the literature~\cite{Lu2022}. Further, by calculating the gradient of the electron density, the corresponding diffusion current was obtained. The result presented a typical transient current structure that radiated outward radially, which was in line with the characteristics of the Photo-Dember current.\newline
To enhance the physical accuracy of the model, additional holes were introduced and a double carrier coupling equation was constructed to reflect the mechanism of net current formation due to the difference in carrier mobilities between electrons and holes. The double carrier model shows that early transport is still dominated by electrons, while in the later stage, the spatial distribution of electrons and holes becomes more overlapping, resulting in a smoother overall carrier distribution, which is consistent with the existing experimental observations.

\subsection{Electric Field Computation}
The numerical computation of the radiated electric field was implemented in MATLAB as a vectorised function that accepts the full three-component current density history $\mathbf{J}(\mathbf{r}', t) = [J_x, J_y, J_z]$ from the carrier transport model and returns the time-domain electric field waveform at a specified observation point. The computational model discretises the semiconductor surface into a grid of elementary Hertzian dipoles, and the total radiated field is obtained by coherently summing the retarded-time contributions from all source elements using the far-field approximation derived in Section~4.2.

\subsubsection{Discretisation and Geometry}
Figure~\ref{fig:geometry} illustrates the computational geometry. The semiconductor surface at $z = 0$ is represented by an $N_x \times N_y$ spatial grid spanning a domain of $L_x \times L_y = 1.5~\mathrm{mm} \times 1.5~\mathrm{mm}$, with the default resolution set to $N_x = N_y = 601$ grid points in the GUI implementation. Each grid cell at position $\mathbf{r}'_{ij}$ carries a time-varying current density $\mathbf{J}(\mathbf{r}'_{ij}, t_k)$ obtained from the diffusion-recombination solver. The observation point $\mathbf{r} = (X_0, Y_0, z_0)$ is placed in the far-field region, typically at $z_0 = 5$--$10$~mm above the surface. For each source cell, the displacement vector $\mathbf{R}_{ij} = \mathbf{r} - \mathbf{r}'_{ij}$ defines the distance $R_{ij} = |\mathbf{R}_{ij}|$ and the unit vector $\hat{\mathbf{R}}_{ij} = \mathbf{R}_{ij}/R_{ij}$, which determines both the retardation delay and the geometric projection of the radiated field. A singularity guard replaces any $R_{ij} = 0$ with machine epsilon to prevent numerical division errors.

The surface integral in Equation~(\ref{eq:farfield}) is then approximated as a discrete sum over all source cells:
\begin{equation}
    \mathbf{E}(\mathbf{r}, t_k) \approx \frac{\mu_0}{4\pi} \sum_{i=1}^{N_x} \sum_{j=1}^{N_y} \frac{\hat{\mathbf{R}}_{ij} \times \left[\hat{\mathbf{R}}_{ij} \times \dot{\mathbf{J}}(\mathbf{r}'_{ij}, t_{\mathrm{ret},ij})\right]}{R_{ij}} \, \Delta x \, \Delta y.
    \label{eq:discrete}
\end{equation}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.75\linewidth]{interim_report_latex_template (1)/figure/yaoqqi/figure_geometry_schematic.png}
    \caption{
        Schematic of the far-field radiation geometry. The semiconductor surface at $z = 0$ is discretised into an $N_x \times N_y$ grid of source cells, each carrying a time-varying current density whose time derivative $\dot{\mathbf{J}}$ (green arrow) acts as an elementary radiation source. The radiated electric field at the observation point $\mathbf{r}$ (red marker) is obtained by summing retarded-time contributions from all source elements along the displacement vectors $R_{ij}$ (dashed lines), with $\hat{\mathbf{R}}_{ij}$ denoting the unit direction from each source to the observer. The inset shows the retarded time relation $t_{\mathrm{ret}} = t - R_{ij}/c$, which accounts for the finite propagation delay of the electromagnetic signal.
    }
    \label{fig:geometry}
\end{figure}

\subsubsection{Three-Stage Numerical Algorithm}
The evaluation of Equation~(\ref{eq:discrete}) is carried out through a three-stage pipeline, as summarised in the flowchart of Figure~\ref{fig:flowchart}. The input to the algorithm is the full spatiotemporal current density history $\mathbf{J}(x_i, y_j, t_k)$ produced by the carrier transport simulation, and the output is the three-component electric field waveform $\mathbf{E}(t)$ at the chosen detector position.\newline
\textbf{Stage 1: Temporal derivative.} The radiated field is proportional to $\dot{\mathbf{J}}$, not $\mathbf{J}$ itself, so the first step computes the time derivative of each current density component at every spatial grid point. A second-order central finite difference scheme is employed:
\begin{equation}
    \dot{J}_x(x_i, y_j, t_k) = \frac{J_x(x_i, y_j, t_{k+1}) - J_x(x_i, y_j, t_{k-1})}{2\Delta t},
\end{equation}
applied identically to $J_y$ and $J_z$. The second-order scheme was chosen over a first-order forward difference to minimise numerical dispersion errors in the derivative, which is critical because $\dot{\mathbf{J}}$ directly determines the amplitude and waveform shape of the radiated pulse. At the boundary time steps ($k = 1$ and $k = N_t$), the derivative is set to zero, as the current density is negligible at the start and end of the simulation window.\newline
Figure~\ref{fig:dJdt} presents a representative snapshot of the resulting $|\dot{\mathbf{J}}|$ distribution across the source grid. The peak of $|\dot{\mathbf{J}}|$ occurs at $t \approx -0.03$~ps, which is a slightly negative time relative to the defined pulse centre at $t = 0$. This is physically expected: the simulation time axis is defined such that $t = 0$ corresponds to the peak of the Gaussian optical excitation pulse, and the time window extends from $T_{\mathrm{min}} < 0$ to $T_{\mathrm{max}} > 0$ to capture the full transient response. The current derivative peaks on the rising edge of the laser pulse because this is when the carrier generation rate—and hence the rate of change of the diffusion current—is increasing most rapidly. By the time the pulse reaches its maximum at $t = 0$, the generation rate has begun to plateau, causing $\dot{\mathbf{J}}$ to decrease. This temporal asymmetry is a fundamental characteristic of the Gaussian excitation profile and is consistent with the bipolar waveform structure observed in the radiated THz pulse.\newline
Spatially, the heatmap reveals a distinctive ring-shaped magnitude distribution centred on the laser excitation spot. The magnitude $|\dot{\mathbf{J}}|$ is largest at intermediate radii where the spatial gradient of the carrier density is steepest, corresponding to the flanks of the Gaussian beam profile. At the beam centre, the carrier density is nearly uniform (the gradient is zero), so $\dot{\mathbf{J}}$ vanishes despite the high absolute carrier concentration. Similarly, far from the excitation region, the carrier density itself is negligible, so the derivative is also zero. The overlaid white arrows confirm that $\dot{\mathbf{J}}$ is directed radially outward at all azimuthal angles, reflecting the isotropic nature of the Photo-Dember diffusion mechanism. This radially symmetric source distribution has a direct physical consequence for the emitted radiation: when the far-field integral in Equation~(\ref{eq:discrete}) is evaluated along the surface normal ($\theta = 0^\circ$), the transverse projections from opposite sides of the ring cancel by symmetry, producing the characteristic on-axis null observed in both simulation and experiment~\cite{Mueckstein2015}.\newline
\textbf{Stage 2: Geometry and retarded time.} For each source cell $(i,j)$ and each observation time $t_k$, the retarded time is computed as $t_{\mathrm{ret},ij} = t_k - R_{ij}/c$, accounting for the finite speed of light $c$ over the source-to-observer distance $R_{ij}$. Since the retarded time generally does not coincide with a discrete simulation time step, it is converted to a fractional array index $\tau = (t_{\mathrm{ret}} - t_1)/\Delta t + 1$, and linear interpolation is applied between the two nearest stored time steps:
\begin{equation}
    \dot{\mathbf{J}}_{\mathrm{ret}} = (1 - \alpha)\,\dot{\mathbf{J}}(\mathbf{r}'_{ij}, t_{\lfloor\tau\rfloor}) + \alpha\,\dot{\mathbf{J}}(\mathbf{r}'_{ij}, t_{\lfloor\tau\rfloor+1}),
\end{equation}
where $\alpha = \tau - \lfloor\tau\rfloor$ is the fractional part. A validity mask is applied to ensure that source cells whose retarded time falls outside the simulation time window ($t_{\mathrm{ret}} < T_{\mathrm{min}}$ or $t_{\mathrm{ret}} > T_{\mathrm{max}}$) contribute zero to the field sum, preventing spurious edge effects.\newline
\textbf{Stage 3: Cross product and accumulation.} The double cross product $\hat{\mathbf{R}} \times [\hat{\mathbf{R}} \times \dot{\mathbf{J}}]$ in Equation~(\ref{eq:discrete}) extracts the transverse component of the current acceleration relative to the line of sight. Rather than computing two sequential cross products, which would require six intermediate multiplications, the implementation exploits the BAC-CAB vector identity:
\begin{equation}
    \hat{\mathbf{R}} \times [\hat{\mathbf{R}} \times \dot{\mathbf{J}}] = (\hat{\mathbf{R}} \cdot \dot{\mathbf{J}})\,\hat{\mathbf{R}} - \dot{\mathbf{J}},
\end{equation}
which reduces the operation to a single dot product and two vector scalings. This formulation is not only computationally more efficient but also numerically more stable, as it avoids the subtraction of nearly equal cross-product terms that can occur at small observation angles. Each resulting transverse component is divided by $R_{ij}$, multiplied by the area element $\Delta x\,\Delta y$, and summed over all $N_x \times N_y$ source cells using fully vectorised MATLAB array operations with linear indexing via \texttt{sub2ind} to avoid nested spatial loops.

\subsubsection{Computational Performance}
The overall computational cost of the far-field calculation scales as $\mathcal{O}(N_x \cdot N_y \cdot N_t)$ per observation point, where $N_t$ is the number of output time steps. For the default parameters used in the GUI implementation ($N_x = N_y = 601$, $N_t = 800$ output time steps over a window from $-5$~ps to $40$~ps), a single-point waveform calculation completes within approximately one minute on a standard desktop computer. The main performance bottleneck is the outer loop over observation times $t_k$, which remains a scalar loop because the retarded time $t_{\mathrm{ret},ij}$ depends on the current value of $t_k$; however, all spatial operations within each time step are fully vectorised. To generate two-dimensional transverse field maps at a fixed propagation distance $z_0$, the computation is repeated over a coarse grid of observation points $(X_0, Y_0)$, which could be further accelerated using MATLAB's parallel computing toolbox if needed.

\begin{figure}[h]
    \centering
    \includegraphics[width=0.75\linewidth]{interim_report_latex_template (1)/figure/yaoqqi/figure_dJdt_snapshot.png}
    \caption{
        Snapshot of the time derivative of the current density $|\partial \mathbf{J}/\partial t|$ across the semiconductor surface at the time of peak current acceleration ($t \approx -0.03$~ps). The heatmap shows the magnitude distribution, which forms a characteristic ring pattern centred on the excitation spot, reflecting the steepest carrier density gradients at the flanks of the Gaussian beam. The white arrows indicate the direction of $\dot{\mathbf{J}}$, confirming radially outward current acceleration consistent with the isotropic Photo-Dember diffusion mechanism. The negative time indicates that peak current acceleration occurs on the rising edge of the femtosecond laser pulse, before the optical intensity reaches its maximum at $t = 0$.
    }
    \label{fig:dJdt}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\linewidth]{interim_report_latex_template (1)/figure/yaoqqi/figure_algorithm_flowchart.png}
    \caption{
        Flowchart of the three-stage far-field electric field computation algorithm. The input current density $\mathbf{J}(x_i, y_j, t_k)$ from the carrier transport model is first differentiated in time (Stage~1, central finite differences) to obtain the radiation source term $\dot{\mathbf{J}}$. For each observation time, the retarded time at every source cell is computed and the derivative is interpolated between discrete time steps (Stage~2). The transverse projection is then evaluated using the BAC-CAB vector identity (Stage~3). All contributions are summed via superposition to produce the output THz electric field waveform $\mathbf{E}(t)$ at the detector position.
    }
    \label{fig:flowchart}
\end{figure}

\subsection{MATLAB GUI Interface Design}
In this project, an integrated MATLAB App Designer GUI was designed and implemented to interface with the physics-based terahertz (THz) photocurrent simulation engine. The main goal is to provide a user-friendly, interactive platform that allows the user to configure material, excitation, and geometric parameters, execute the full 2D diffusion-recombination mode, and visualize the resulting carrier dynamics and THz waveforms.\newline
The overall GUI structure uses a modular, panel-based layout. Four functional tabs were implemented, including Mechanism Selector, Numeric Fields, Excitation, and $E(t)$ position. These tabs allow users to select the physical mechanism (built-in field, photo-Dember effect, or both), adjust semiconductor parameters (mobility, lifetime, absorption depth, temperature), configure optical excitation (pulse width, fluence, spot size, incidence angle, pulse shape), and specify the THz observation point in 3D space. A separate 3D axes window is implemented to visualize the rectangular semiconductor and the laser beam geometry, including features like incidence-angle-dependent beam rotation.\newline
Callback functions were developed that gather all GUI inputs, validate them, and package them into a structured parameter set for the simulation core. This includes converting fluence to peak intensity, mapping spot size to Gaussian waist, computing Einstein-relation diffusion coefficients, and applying incidence-angle correlations. The GUI supports additional physics toggles that enable the photo-Dember current and built-in drift current to be selectively activated.\newline
The GUI was integrated with the simulation backend, which returns the carrier density evolution, peak-density spatial distribution, and the THz electric field waveform computed via time-derivative current radiation. These results are visualized in three coordinated plots, including central carrier density vs. time, carrier distribution at the peak time, and $\lvert E \rvert (t)$ waveform at a specific chosen observation point.\newline
Consistent axis labels, physical units $(\mathrm{V/m},\ \mathrm{m^{-3}},\ \mu\mathrm{m})$, dynamic titles, and error-handling alerts were added to ensure robustness and scientific clarity.

%% ============================================================
%% RESULTS AND DISCUSSION
%% ============================================================
\section{Results and Discussion}

\subsection{Photo-Dember Model Results}
% TODO: Present and critically evaluate your Photo-Dember model results.
% Include figures, analysis, and comparison with literature.
Overall, the numerical platform has been established and verified, laying a solid foundation for subsequent calculations of terahertz radiation based on transient currents.
\begin{figure}[h]
	\begin{center}
	\includegraphics[width=14.5cm]{interim_report_latex_template (1)/figure/yuhao/image.png}
	\caption{
            Electron density heat map with vectors indicate current
	}
	\label{Electron density heat map with vectors indicate current}
	\end{center}
\end{figure}

\subsection{Electric Field Results}
The time-dependent behaviour of the radiated terahertz electric field was evaluated at a fixed far-field observation point located at $x = -20$ $\mu$m, $y = 30$ $\mu$m, $z = 10$ mm. Figure 2 presents the corresponding temporal traces of the $E_x$ and $E_y$ components. Both components display a distinctly bipolar pulse profile. This bipolar structure of the simulated field is in strong accordance with the theoretical model developed by Auston et al.\cite{auston_picosecond_1984}. In their pioneering study on picosecond photoconductors, Auston and co-workers showed that the far-field radiation produced by a transient current source is proportional to the first time derivative of the dipole moment, $\mathbf{E} \propto \dot{\mathbf{p}}(t)$. The results obtained here reproduce this anticipated behaviour: the leading positive part of the waveform is associated with the rapid rise of the photocurrent, whereas the following negative part is linked to the current decay. This confirms that the numerical implementation of the retarded potential accurately reflects the fundamental connection between carrier acceleration and electromagnetic radiation.
\begin{figure}[h]
	\begin{center}
	\includegraphics[width=9.5cm]{interim_report_latex_template (1)/figure/yaoqqi/field2d.png}
	\caption{
		Temporal waveforms of the radiated THz electric field components $E_x(t)$ (top) and $E_y(t)$ (bottom) calculated at an observation point of $(-20 \mu\text{m}, 30 \mu\text{m}, 10 \text{mm})$.
	}
	\label{field2d}
	\end{center}
\end{figure}

To characterise the polarisation of the emitted beam, the instantaneous transverse vector field distribution in the $xy$-plane was examined at a propagation distance of $z = 5$ mm and a time of $t = 0.2$ ps. As illustrated in Figure 3, the electric field vectors display clear radial symmetry, directed outward from the propagation axis. This radial polarisation pattern is a direct consequence of the isotropic Photo-Dember diffusion currents~\cite{Reklaitis2010,Klatt2010}, and is consistent with the angular emission characteristics measured by Mueckstein et al.~\cite{Mueckstein2015}. Analogous radially polarised THz emission has recently been observed in emerging material systems, where terahertz emission spectroscopy has become a versatile probe of ultrafast symmetry-breaking processes~\cite{Pettine2023,Wu2024}.
\begin{figure}[h]
	\begin{center}
	\includegraphics[width=8.5cm]{interim_report_latex_template (1)/figure/yaoqqi/fielddistr.png}
	\caption{
		Instantaneous vector map of the THz electric field $(E_x, E_y)$ in the transverse plane at $z = 5$ mm. The field exhibits radial polarisation, consistent with the isotropic diffusion of photo-carriers from the excitation centre.
	}
	\label{fielddistr}
	\end{center}
\end{figure}

\subsection{GUI Interface Results}
% TODO: Present the GUI results with critical evaluation.
% How well does it work? Performance analysis?
\begin{figure}[h]
	\begin{center}
	\includegraphics[width=12.5cm]{interim_report_latex_template (1)/figure/shenglong/parameter.png}
	\caption{
		Parameters Selection Tabs
	}
	\label{Parameters Selection Tabs}
	\end{center}
\end{figure}

\begin{figure}[h]
	\begin{center}
	\includegraphics[width=12.5cm]{interim_report_latex_template (1)/figure/shenglong/3d.png}
	\caption{
		3D Semiconductor and Beam Visualization
	}
	\label{3D Semiconductor and Beam Visualization}
	\end{center}
\end{figure}

\begin{figure}[h]
	\begin{center}
	\includegraphics[width=12cm]{interim_report_latex_template (1)/figure/shenglong/central.png}
	\caption{
		Central Carrier Density Plot
	}
	\label{Central Carrier Density Plot}
	\end{center}
\end{figure}

\begin{figure}[b]
	\begin{center}
	\includegraphics[width=12cm]{interim_report_latex_template (1)/figure/shenglong/carrier.png}
	\caption{
		Carrier Distribution Plot at Peak Time
	}
	\label{Carrier Distribution Plot at Peak Time}
	\end{center}
\end{figure}
\begin{figure}[h]
	\begin{center}
	\includegraphics[width=12.5cm]{interim_report_latex_template (1)/figure/shenglong/Thz.png}
	\caption{
		THz Field at Specific Chosen Position and Incidence Angle
	}
	\label{THz Field at Specific Chosen Position and Incidence Angle}
	\end{center}
\end{figure}

\begin{figure}[h]
	\begin{center}
	\includegraphics[width=12.5cm]{interim_report_latex_template (1)/figure/shenglong/complete.png}
	\caption{
		Complete GUI Overview
	}
	\label{Complete GUI Overview}
	\end{center}
\end{figure}

%% ============================================================
%% CONCLUSIONS
%% ============================================================
\section{Conclusions}
% TODO: Write conclusions that summarise the main findings.
% - What is your main project result?
% - What did you achieve?
% - What lessons were learned?
% This replaces the old "Project management" and "Future work" sections.

%% ============================================================
%% TEAM CONTRIBUTIONS
%% ============================================================
\section{Team Contributions}
% TODO: List team member contributions and indicate who wrote which sections.
%
% Yaoqi Dong -- Photo-Dember effect modelling (1D, 2D, dual-carrier)
% Yuhao Chang -- Electric field computation (retarded potentials, 2D/3D field simulation)
% Shenglong Li -- MATLAB GUI design and integration

\newpage
\bibliography{example-references}
\bibliographystyle{IEEEtran} % IEEE style



\end{document}
